# Production EC2 overrides — no extra containers, no dev mounts
services:
  mysql:
    ports: []

  redis:
    ports: []

  backend:
    volumes:
      - ./.env:/app/.env:ro

  frontend:
    volumes:
      - ./.env:/app/.env:ro
